<div class="row">
  <div class="col-lg-10 col-lg-offset-1">
    
    <h1><%= @page.title %></h1>
    <p><%= @page.created_at.strftime("%m/%d/%Y") %></p>
    <%= RDiscount.new(@page.content).to_html %>
    
  </div>
</div>